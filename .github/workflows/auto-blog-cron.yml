name: 🤖 Auto Blog Cron (Every 30 Min)

on:
  schedule:
    # Har 30 minute pe chalega — GitHub Actions FREE mein unlimited
    # UTC time mein hai: 05:30 UTC = 11:00 IST, 06:00 UTC = 11:30 IST, etc.
    - cron: "0,30 * * * *"

  # Manual trigger bhi possible hai (GitHub → Actions → Run workflow)
  workflow_dispatch:
    inputs:
      reason:
        description: "Manual trigger reason"
        required: false
        default: "Manual test run"

jobs:
  trigger-auto-blog:
    name: Trigger Auto Blog Scraper
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: 📡 Trigger Auto Blog Cron API
        id: trigger
        run: |
          echo "⏰ Triggering auto blog cron at $(date -u '+%Y-%m-%d %H:%M:%S UTC')"

          HTTP_STATUS=$(curl -s -o /tmp/response.txt -w "%{http_code}" \
            --max-time 300 \
            --retry 2 \
            --retry-delay 10 \
            -X GET \
            "https://www.rojgarsuvidha.com/api/auto-blog/cron?key=${{ secrets.CRON_SECRET || 'rojgarsuvidha_auto_blog_2026' }}")

          echo "HTTP Status: $HTTP_STATUS"
          echo "Response: $(cat /tmp/response.txt)"

          if [ "$HTTP_STATUS" = "200" ]; then
            echo "✅ Cron triggered successfully!"
          else
            echo "❌ Cron failed with HTTP $HTTP_STATUS"
            exit 1
          fi

      - name: 📊 Log Result
        if: always()
        run: |
          echo "Run completed at $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
